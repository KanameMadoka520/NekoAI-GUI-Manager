# 给下一个开发者的交接说明

## 当前状态概览

这轮开发已经完成并提交了四组核心更新：

1. **人格评测实验室首版**
   - 独立页面已接入
   - 支持多 API / 多人格 / 多用例 / 多轮次实验矩阵
   - 支持单条输出多维人工打分
   - 支持基础统计摘要
   - 支持实验记录落盘、回看、删除
   - 已通过本地 `npm run build` 与用户环境 `npx tauri build`

2. **人格 A/B 测试台记录管理增强**
   - 支持测试记录搜索
   - 支持模式筛选
   - 支持删除记录
   - 保持 A/B 测试台作为“快速试跑 / 草稿验证”工具

3. **配置契约与迁移治理**
   - 插件目录新增 `runtime_schema.json`
   - GUI 已接入 schema，用于字段说明、约束校验、迁移提醒和部分表单渲染
   - 配置页新增“迁移提醒”“差异概览”“自动修复可修项”
   - API 健康评分权重已迁移到 GUI 本地数据目录，不再写入插件 `runtime_config.json`
   - 新增独立“用量管理”页，用于管理 `group_usage_counts.json`

4. **文档与交接同步**
   - `README.md`
   - `CONTRIBUTING.md`
   - `开发交接文档-当前开发目标.md`
   - `.gitignore`

---

## 重要提交（建议先看）

建议先按这个顺序看最近提交：

1. `960fefe`
   **实现人格评测实验室首版并同步文档**

2. `f6ed93f`
   **完善人格 A/B 测试记录管理**

3. `b661a21`
   **更新交接文档与本地忽略规则**

---

## 当前产品分工（很重要）

### 1. 人格 A/B 测试台
定位：
- 快速试跑
- 草稿验证
- 临时比对

不要再继续往里面堆：
- 多 API 严谨实验
- 多轮统计分析
- 复杂评测报表

### 2. 人格评测实验室
定位：
- 正式实验
- 多 API / 多轮次对比
- 人工打分沉淀
- 统计分析

后续严谨评测相关功能，应继续加在这里，而不是回灌到 A/B 测试台。

---

## 关键文件入口

### 前端
- `src/App.tsx`
- `src/components/layout/Sidebar.tsx`
- `src/hooks/useKeyboardShortcuts.ts`
- `src/lib/human-issues.ts`
- `src/pages/PersonalityManager.tsx`
- `src/components/personality/PersonalityAbWorkbench.tsx`
- `src/pages/EvaluationLab.tsx`
- `src/pages/UsageManager.tsx`
- `src/pages/ConfigEditor.tsx`
- `src/lib/types.ts`
- `src/lib/tauri-commands.ts`

### Rust / Tauri
- `src-tauri/src/main.rs`
- `src-tauri/src/personality_ab.rs`
- `src-tauri/src/personality_eval.rs`
- `src-tauri/src/data_root.rs`
- `src-tauri/src/gui_prefs.rs`
- `src-tauri/src/config.rs`
- `src-tauri/src/ops.rs`

### 文档
- `README.md`
- `CONTRIBUTING.md`
- `开发交接文档-当前开发目标.md`

---

## 数据落盘位置

运行时数据继续放在 EXE 同级：

`NekoAI-GUI-Data/`

其中本轮相关目录：
- `preferences/`：GUI 本地偏好（如 API 健康评分权重）
- `personality-ab-tests/`：A/B 快速试跑记录
- `personality-eval-lab/experiments/`：评测实验室实验记录

不要把这些数据改到别的位置。

---

## 当前已知下一步优先事项

建议下一阶段优先做：

1. **人格评测实验室真实数据回归**
   - 多 provider 实际测试
   - 多人格 / 多用例 / 多轮次实际验证
   - 检查评分保存、实验回看、删除、统计是否都符合预期

2. **人格评测实验室交互增强**
   - 更细粒度筛选
   - 更好的结果定位与浏览
   - 更明确的统计说明

3. **人格评测实验室统计增强**
   - 图表化展示
   - 导出能力
   - 更完整的报表

4. **A/B 测试台稳定性回归**
   - 在真实 API / 不同 provider 下继续检查快速试跑流程

5. **GUI 全局文案巡检继续收尾**
   - 当前已经完成 Setup / Dashboard / ApiManager / PersonalityManager / CommandManager / MemoryViewer / UsageManager / HistoryViewer / OpsCenter 的一轮“人类可读性”改写
   - 后续如果继续做 GUI，请保持“技术标签 + 人话解释 + 下一步建议”的表达方式，不要再回到日志腔

---

## 本地验证建议

至少执行：

```bash
npm run build
```

如涉及 Rust / Tauri：

```bash
npx tauri build
```

---

## 额外提醒

- `开发交接文档-当前开发目标.md` 现在已经纳入版本控制，不要再忽略它。
- `.claude/` 已加入 `.gitignore`，本地辅助内容不需要提交。
- 如果继续做人格评测实验室，请先保持“实验室负责严谨评测，A/B 负责快速试跑”这个边界，不要重新混回去。
