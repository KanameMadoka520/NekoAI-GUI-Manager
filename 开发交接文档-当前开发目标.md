# NekoAI GUI Manager 开发交接文档（当前开发目标）

> 用途：下次会话开始时，先读本文件，即可快速恢复上下文并继续开发。
>
> 适用范围：`/app/NekoAI GUI Manager`
>
> 更新时间：2026-03-10（已同步：人格 A/B 测试台管理增强、人格评测实验室首版实现、UI 缺口修复与本地构建验证）

---

## 1. 项目定位（必须先统一）

- 本项目是 **NekoAI GUI Manager**（Tauri + React 的桌面可视化工具）。
- 同仓库存在插件源码目录（如 `koishi-plugin-TCY-nekoAI`），但当前开发任务聚焦 GUI，不开发插件本体。
- GUI 的核心职责：可视化编辑插件配置、查看记忆与历史、执行管理与治理功能。

---

## 2. 已落地能力（当前基线）

这部分是“已经有代码”的基线，后续迭代不能破坏。

### 2.1 安全发布中心（Phase1+2 MVP）

前端页面：
- `src/pages/OpsCenter.tsx`

后端能力：
- `src-tauri/src/ops.rs`
- `src-tauri/src/main.rs`（命令注册）
- `src/lib/tauri-commands.ts`（前端 invoke 封装）
- `src/lib/types.ts`（相关 TS 类型）

已支持：
1. 配置快照：创建、列表、回滚、双快照差异
2. 部署包导出：导出 runtime/api/personality/commands + manifest
3. 环境模板：dev/test/prod 保存、预览、应用（应用后自动快照）
4. 启动前自检：检查缺失文件、JSON 解析问题、索引/类型问题，可修项自动修复
5. GUI 审计日志：关键操作写入并在 UI 展示

### 2.2 EXE 同级数据目录（关键约束）

实现文件：
- `src-tauri/src/data_root.rs`

机制：
- 运行时以 `current_exe()` 为基准，在 EXE 同级创建 `NekoAI-GUI-Data/`
- 子目录：
  - `snapshots/`
  - `deploy-packages/`
  - `env-templates/`
  - `diagnostics/`
  - `audit/`
  - `personality-ab-tests/`

**强约束：后续所有“安全保存/治理类数据”继续放这个根目录，不改到其它位置。**

### 2.3 其它近期已落地改动（与后续可能相关）

- API 批量测试流式回传
- API 治理（P0）已完成两项 MVP：
  - 智能路由策略编辑器（主模型/备用模型/降级策略/重试参数/策略预览）
  - 节点评分系统（实时测试 + 历史表现）并支持健康分排序/筛选
- API 治理（P0）增强已完成：
  - 评分解释面板（实时/历史 分值构成 + 主要扣分原因）
  - 实时/历史 权重滑杆（并写入 runtime.apiHealthWeights）
  - 第二轮增强：补充超时率/抖动维度、多权重组合（实时/历史/超时/抖动）与解释进度条
- 安全提示增强已完成：
  - API 管理页、安全发布中心都加入“分享前移除 api_config.json”警示，降低 Key 泄露风险
- 前端性能第二轮优化已完成（ApiManager）：
  - 节点卡片改为 memo，降低批量测试、筛选、拖拽外的重复渲染
- API 管理布局优化已完成（当前一轮）：
  - 新增顶部摘要栏、主/次工具栏分层、API key栏按节点/全部展开收纳与更图形化的评分解释面板
- HistoryViewer 布局优化已完成（当前一轮）：
  - 搜索模式改为主搜索条 + 可折叠高级筛选面板，降低顶部筛选区拥挤感
- ConfigEditor 布局优化已完成（当前一轮）：
  - 新增“常用 / 完整”模式、顶部摘要栏与章节说明，降低长表单编辑压力
- PersonalityManager 布局优化已完成（当前一轮）：
  - 新增顶部状态摘要、导入导出收纳区与更清晰的人格卡片层级
- Personality A/B 测试台 MVP 已完成（当前一轮）：
  - 支持群聊/私聊同模式人格 A/B 对比
  - 支持指定固定 API 节点执行测试
  - 支持保存测试记录到 `NekoAI-GUI-Data/personality-ab-tests/`
  - 支持最近记录回看、模式筛选、关键词搜索与单条删除
  - 当前定位是“快速试跑 / 草稿验证”工具，不承载严谨实验统计
- Personality 评测实验室首版已完成（当前一轮）：
  - 已新增独立页面入口（侧栏 / 快捷键 / 页面接入）
  - 支持多 API × 多人格 × 多用例 × 多轮次实验矩阵
  - 支持单条输出人工打分（风格一致性 / 稳定性 / 任务完成度 / 拟人感）与备注保存
  - 支持基础统计摘要（按人格 / API / 组合）
  - 支持实验记录落盘到 `NekoAI-GUI-Data/personality-eval-lab/experiments/`
  - 已补齐首轮 UI 缺口：模式切换后人格选择校正、加载实验同步顶部配置、前端规模限制提示、评分保存中状态、实验记录摘要增强
  - 已通过 `npm run build` 与用户本地 `npx tauri build`
- MemoryViewer 布局优化已完成（当前一轮）：
  - 新增顶部摘要、会话列表说明与详情页工具收纳区，降低记忆治理视图拥挤感
- OpsCenter 布局优化已完成（当前一轮）：
  - 新增顶部摘要、面板说明与可折叠工具区，统一安全治理页面层级
- Dashboard 布局优化已完成（当前一轮）：
  - 新增顶部摘要条、统一面板说明与概览区块层级
- 公共组件抽取已完成一轮：
  - 已新增 `src/components/common/Panel.tsx` 与 `src/components/common/SummaryCard.tsx`
  - 已统一接入 Dashboard / ApiManager / HistoryViewer / ConfigEditor / PersonalityManager / MemoryViewer / OpsCenter
- Setup 页面路径示例修正 + 打开目录按钮
- 长期记忆详情解析兼容序列化字符串
- 历史记录新增输入/输出/上下文/响应时间展示
- 历史纯图片消息兜底文案
- 历史时间线点击定位

---

## 3. 当前开发目标（下一阶段）

> 本节是“接下来明确要做什么”的执行基线。

## 3.1 总目标

在既有安全发布能力基础上，分阶段补齐三大方向：

1. API 治理类（P0）
2. Prompt/人格工程类（P1）
3. 长期记忆治理类（P2）

并保持：
- 数据落盘路径不变（`NekoAI-GUI-Data/`）
- 不破坏已上线能力
- 每项先 MVP，再增强

### 3.2 目标 A：API 治理类（P0，优先做）

#### A1 智能路由策略编辑器（可视化）

状态：✅ **MVP 已完成**。

已落地点：
- `src/pages/ConfigEditor.tsx`
- `src/lib/types.ts`

当前能力：
- 可配置主模型索引、备用索引列表、降级触发策略、最大切换次数、重试参数
- 已有策略预览（命中顺序/降级路径）
- 兼容旧版 `smartRouter` 字段（通过归一化补齐默认值）

后续增强（可选）：
- 增加策略冲突校验提示（如主索引与备用重复）
- 增加“按场景路由规则”可视化编辑

#### A2 节点评分系统

状态：✅ **MVP 已完成**。

已落地点：
- 前端：`src/pages/ApiManager.tsx`
- 类型：`src/lib/types.ts`
- 后端聚合命令：`src-tauri/src/history.rs#get_api_history_metrics`
- 命令注册：`src-tauri/src/main.rs`
- 前端调用封装：`src/lib/tauri-commands.ts`

当前能力：
- 评分输入：实时 ping 结果 + 历史聚合指标（调用数/错误率/平均响应）
- 输出：健康分（0-100）+ 等级（healthy/warning/risk）
- 交互：API 管理页支持“按健康分排序”与“按等级筛选”

后续增强（可选）：
- 权重可配置化
- 新增超时率/抖动维度
- 评分解释面板（可视化拆分每项得分）

### 3.3 目标 B：Prompt/人格工程类（P1）

#### B1 人格 A/B 测试台（快速试跑工具）

状态：✅ **MVP 已完成**。

已落地点：
- 前端：`src/pages/PersonalityManager.tsx`
- 前端组件：`src/components/personality/PersonalityAbWorkbench.tsx`
- 类型：`src/lib/types.ts`
- 前端调用封装：`src/lib/tauri-commands.ts`
- 后端：`src-tauri/src/personality_ab.rs`
- 命令注册：`src-tauri/src/main.rs`

当前能力：
- 同模式对比：群聊 vs 群聊，私聊 vs 私聊
- 固定 API 节点选择：A/B 两边共用同一个指定节点
- 输入结构：上下文文本（可选）+ 最新用户消息（必填）
- 本地记录：保存到 `NekoAI-GUI-Data/personality-ab-tests/`
- 可回看最近测试记录、模式筛选、关键词搜索与单条删除
- 当前定位：用于“快速试跑 / 草稿验证 / 临时比对”

后续说明：
- **不要继续把多 API、多轮次、人工多维打分、统计报表堆进这个工具。**
- 这部分已经转移到下一个独立页面规划：**人格评测实验室**。

#### B2 人格评测实验室（独立页面，首版已落地）

状态：✅ **首版已完成并通过本地构建验证**。

已落地点：
- 前端：`src/pages/EvaluationLab.tsx`
- 页面接入：`src/App.tsx`
- 导航：`src/components/layout/Sidebar.tsx`
- 快捷键：`src/hooks/useKeyboardShortcuts.ts`
- 类型：`src/lib/types.ts`
- 前端调用封装：`src/lib/tauri-commands.ts`
- 后端：`src-tauri/src/personality_eval.rs`
- 命令注册：`src-tauri/src/main.rs`

当前能力：
- 独立页面，不再继续堆到当前人格管理页中
- 支持多 API × 多人格 × 多用例 × 多轮次实验矩阵
- 支持单条输出人工打分（4 维 + 备注）
- 支持实验记录回看与删除
- 支持按人格 / API / 人格×API 的基础统计摘要
- 已补齐首轮 UI 细化：模式切换时人格选择校正、加载实验同步顶部配置、前端规模限制提示、评分保存中状态、实验记录摘要增强
- 实验记录写入 `NekoAI-GUI-Data/personality-eval-lab/experiments/`
- 已通过 `npm run build` 与用户本地 `npx tauri build`

下一阶段继续补强：
- 真实数据回归
- 更细粒度筛选与交互优化
- 统计图表/导出增强

#### B3 人格冲突检测器

目标：检测 prompt 矛盾、冗余、过长。

MVP 建议：
- 规则引擎先行（关键词冲突、重复条款、长度阈值）
- 产出问题列表，不自动改写

#### B4 人格模板市场（本地）

目标：建立本地人格模板库，支持一键套用和参数化。

MVP 建议：
- 模板列表（名称、版本、标签）
- 导入/导出模板
- 参数占位符替换（例如 `{{nickname}}`）

建议数据目录：
- `NekoAI-GUI-Data/personality-lab/`
  - `conflict-reports/`
  - `template-market/`

### 3.4 目标 C：长期记忆治理类（P2）

#### C1 记忆压缩与归档助手

目标：按规则对旧记忆做摘要归档，降低噪音。

MVP 建议：
- 配置阈值（条数/时间）
- 一键压缩（先 dry-run，再执行）
- 归档可回看

#### C2 记忆质量评分

目标：识别低信息密度、重复、冗余条目。

MVP 建议：
- 输出每条 memory score
- 提供“建议删除/保留”标签

#### C3 记忆可视化关系图

目标：按用户/主题/关键词展示记忆簇。

MVP 建议：
- 先做“聚类列表 + 关键词摘要”，再演进图形化

建议数据目录：
- `NekoAI-GUI-Data/memory-governance/`
  - `archives/`
  - `quality-reports/`
  - `clusters/`

---

## 4. 建议执行顺序（防止发散）

### Wave 1（先做）
1. API 治理：P0 增强回归与权重细化
2. Prompt/人格：人格评测实验室首版回归与增强

### Wave 2
3. Prompt/人格：人格冲突检测器 MVP
4. Prompt/人格：本地模板市场 MVP

### Wave 3
5. 记忆治理：压缩归档 MVP
6. 记忆治理：质量评分 MVP

### Wave 4
7. 记忆治理：关系图 MVP
8. 评测实验室图表/导出类增强

---

## 5. 实施约束（必须遵守）

1. **路径约束**
   - 安全保存相关数据只写 `NekoAI-GUI-Data/`，禁止散落到临时目录。

2. **兼容约束**
   - 不破坏当前 `OpsCenter` 的已实现能力。
   - 不做破坏性字段更名（除非有迁移脚本 + 文档）。
   - 不破坏当前人格 A/B 测试台的可用性。

3. **交互约束**
   - 导入/覆盖类动作保持确认机制。
   - 高风险动作（批量覆盖、回滚）必须给明确反馈。

4. **文档约束**
   - 每次完成子功能，必须同步更新：
     - `README.md`
     - `CONTRIBUTING.md`
     - 本交接文档

---

## 6. 下次会话快速启动指令（给下一位开发者）

建议下一次直接按以下流程执行：

1. 先读本文件：`NekoAI GUI Manager/开发交接文档-当前开发目标.md`
2. 再读：
   - `NekoAI GUI Manager/README.md` 的“当前开发目标（下一阶段）”
   - `NekoAI GUI Manager/CONTRIBUTING.md` 的“当前开发目标（下一阶段，执行基线）”
3. 当前优先方向：
   - 先检查 P0 评分增强与回归项是否还要补
   - 然后优先推进 **人格评测实验室** 独立页面，不要再往现有 A/B 测试台里堆严谨评测逻辑
4. 开发完成后先跑：
   - `npm run build`
   - （可行时）`npx tauri build`
5. 更新三份文档并记录“已完成/待回归项”。

---

## 7. 当前未完事项（明确保留）

- P0 已完成 MVP（A1 智能路由编辑器、A2 节点评分）及第一轮增强（解释面板/权重）。
- 当前最值得优先推进的是：
  - P0 评分权重细化（加入超时率/抖动与可解释分项图表的继续回归）
  - P1 人格评测实验室首版真实数据回归、筛选细化与统计导出增强
  - 人格 A/B 测试台在真实 API/provider 下的稳定性回归

---

## 8. 关键代码入口索引

- 页面入口：`src/App.tsx`
- 安全发布中心：`src/pages/OpsCenter.tsx`
- API 管理：`src/pages/ApiManager.tsx`
- 配置编辑：`src/pages/ConfigEditor.tsx`
- 人格管理：`src/pages/PersonalityManager.tsx`
- 人格评测实验室：`src/pages/EvaluationLab.tsx`
- 人格 A/B 快速试跑组件：`src/components/personality/PersonalityAbWorkbench.tsx`
- 长期记忆：`src/pages/MemoryViewer.tsx`
- 历史记录：`src/pages/HistoryViewer.tsx`
- TS 类型：`src/lib/types.ts`
- Tauri 命令封装：`src/lib/tauri-commands.ts`
- 命令注册：`src-tauri/src/main.rs`
- 数据根目录：`src-tauri/src/data_root.rs`
- 安全发布后端：`src-tauri/src/ops.rs`
- 配置读写：`src-tauri/src/config.rs`
- API 测试：`src-tauri/src/api_test.rs`
- 历史处理：`src-tauri/src/history.rs`
- 人格 A/B 后端：`src-tauri/src/personality_ab.rs`
- 人格评测实验室后端：`src-tauri/src/personality_eval.rs`
- 下一阶段规划后端入口：`src-tauri/src/personality_eval_export.rs`（如后续要做报表导出，可再新增；当前未创建）

---

如需继续开发，请直接告诉助手：

“读取 `NekoAI GUI Manager/开发交接文档-当前开发目标.md`，先检查 P0 增强项与人格评测实验室规划，再继续开发。”
