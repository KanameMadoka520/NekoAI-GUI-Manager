# NekoAI GUI Manager — 产品路线图

> 更新日期：2026-07-21  
> 当前版本：`v1.0.4`  
> 用途：记录**已落地能力**与**下一阶段优先事项**，作为迭代方向的单一参考（不再使用「交接文档」命名）。

---

## 1. 当前基线（已发布）

**技术栈**

| 层级 | 方案 |
|------|------|
| 桌面壳 | Tauri v1（Rust `tauri 1.8`），自定义无边框标题栏 |
| 前端 | React 19 + TypeScript + Vite 7 |
| 样式 | Tailwind v4 + CSS 变量设计令牌（`src/index.css` 为真源） |
| 主题 | `data-theme`：`light` / `dark` / `parchment`；另有 lite 低性能模式、density、uiScale |

**构建**

```bash
npm install
npm run build          # 前端
npx tauri build        # 产出 exe + msi + nsis
```

**产物路径**

- 可执行：`src-tauri/target/release/NekoAI Manager.exe`
- 安装包：`src-tauri/target/release/bundle/msi/`、`.../bundle/nsis/`

**发布**

- GitHub Releases：见仓库 [Releases](https://github.com/KanameMadoka520/NekoAI-GUI-Manager/releases)
- 桌面包由维护者本地打包后上传；**不**自动部署到 Koishi / 插件目录

---

## 2. 已完成里程碑

### 2.1 核心功能与运维（~v1.0.x）

- 概览仪表盘、API 管理（聊天 / 图像节点）、配置编辑、用量 / 历史、记忆浏览
- 人格管理、人格 A/B 试跑台、人格评测实验室（首版）
- 安全发布中心 Phase1+2：快照、部署包、环境模板、自检、审计
- 智能路由策略编辑器 MVP、节点健康评分 MVP
- 安全数据统一落盘：`NekoAI-GUI-Data/`（与 EXE 同级）

### 2.2 桌面体验与稳定性

- 边缘缩放拖动（JS 驱动，Tauri v1 无原生 startResize）
- 无目录只读 / **演示模式**（`demo-data` + 读写闸门，未连接插件目录也可预览）
- 显示设置独立 OS 窗口（应用内弹窗回退）+ 跨窗口设置同步
- 修复：React #310（`AmbientFx` Hooks 顺序）、无目录白屏、Windows 透明窗口整窗不可见（已回退透明圆角）
- 窗口拖动 / 缩放期间降级重绘（`data-window-busy`）

### 2.3 UI 体系（v1.0.4 重点）

- 指挥控制台 / 启动器风 HUD：设计令牌、外壳与面板原语全局级联
- 主题进化：粉彩公连默认主题、暗色靛蓝 + 双强调、鎏金羊皮纸、蔚蓝档案（BA）界面语言
- 页内子标签：用量 / 历史 / 发布中心 / 配置编辑等长页分段
- API 页：可收回抽屉、节点目录独立滚动、操作上移节点表头
- 全局文案人性化；默认缩放 110%（含老设置迁移）
- 移除壁纸 / 部分冗余显示项，界面更干净

---

## 3. 下一阶段路线图

优先级与 README「当前开发目标」、CONTRIBUTING §16 对齐。  
原则：**先 MVP，再增强**；不破坏快照 / 部署 / 模板 / 自检 / 审计。

### P0 — API 治理（增强为主）

| 项 | 状态 | 说明 |
|----|------|------|
| 智能路由策略编辑器 | ✅ MVP | 主/备模型、降级与重试、策略预览；保持 `smartRouter` 兼容 |
| 节点健康评分 | ✅ MVP | 批量测试 + 历史指标 → 0–100 分与分档；可排序 / 筛选 |
| 评分权重细化 | 🔜 待办 | 超时 / 抖动等维度、权重可配置 |
| 路由策略可观测性 | 🔜 待办 | 命中路径日志、失败原因摘要（不重做 MVP） |

### P1 — Prompt / 人格工程

| 项 | 状态 | 说明 |
|----|------|------|
| 人格 A/B 试跑台 | ✅ MVP | 快速对比；不继续堆复杂评测统计 |
| 人格评测实验室 | ✅ 首版 | 多 API × 人格 × 用例矩阵、人工打分、基础摘要；落盘 experiments |
| 实验室增强 | 🔜 进行中 | 真实数据回归、交互打磨、统计图 / 导出 |
| 人格冲突检测器 | ❌ 未开始 | 矛盾指令 / 超长 / 重复规则；输出清单与建议（不自动改原文） |
| 人格模板市场（本地） | ❌ 未开始 | 模板库、一键套用、参数化、导入导出与版本 |

### P2 — 长期记忆治理

| 项 | 状态 | 说明 |
|----|------|------|
| 记忆压缩与归档助手 | ❌ 未开始 | 规则摘要旧记忆；可配置阈值与范围 |
| 记忆质量评分 | ❌ 未开始 | 冗余 / 重复 / 低信息密度；可操作清理建议 |
| 记忆可视化关系图 | ❌ 未开始 | 按用户 / 主题 / 关键词成簇；辅助清理 |

### P3 — 桌面壳与体验（可选）

| 项 | 状态 | 说明 |
|----|------|------|
| Win11 原生 DWM 圆角 | 🔜 可选 | 用 `DwmSetWindowAttribute`，**不**再使用 `transparent:true` |
| 边缘缩放手感 | 🔜 可选 | 把手尺寸 / 与关闭按钮、右侧栏避让微调 |
| 本地文件壁纸（若再引入） | ❌ 搁置 | 需 asset 协议 + fs 能力；当前已移除壁纸功能 |

### 建议执行顺序

1. API 治理：仅做权重与可观测性细化，不重做 MVP  
2. 人格：实验室增强 → 冲突检测器 → 本地模板市场  
3. 记忆：压缩归档 → 质量评分 → 关系图  
4. 桌面壳美化仅在不影响稳定性时插入

---

## 4. 硬约束

1. **数据落盘**：安全相关数据仍在 `NekoAI-GUI-Data/`（EXE 同级），勿改路径约定。  
2. **不破坏现有能力**：快照、部署包、环境模板、自检、审计必须可用。  
3. **性能**：`backdrop-filter` 仅用于少数固定外壳；长列表 / 表格行禁止逐行 blur；动画优先 `transform` / `opacity`；lite 模式可彻底降级玻璃与特效。  
4. **窗口属性**：改 `transparent` / decorations 等必须在真实 Windows + WebView2 上验证；曾因透明圆角导致整窗不可见。  
5. **构建**：打包前结束正在运行的 `NekoAI Manager`，避免 exe 占用写失败。  
6. **文档同步**：能力落地后更新 README、CONTRIBUTING 与本 `ROADMAP.md`。  
7. **不自动部署**到用户 Koishi / 插件目录；由用户自行安装发布包。

---

## 5. 验收口径（阶段性）

| 方向 | 口径 |
|------|------|
| API 治理 | 可配置、可执行、可观测（评分 / 排序 / 筛选） |
| 人格工程 | 可对比、可评估、可沉淀（有记录） |
| 记忆治理 | 可压缩、可评分、可视化可辅助清理 |
| 发布质量 | `npm run build` 通过；关键改动经 `npx tauri build`；桌面壳改动有实机确认 |

**状态标记约定**

- ✅ 已完成：代码合并 + 至少一轮可用验证  
- 🔜 进行中 / 待办：有明确范围，尚未交付或仅部分完成  
- ❌ 未开始：仅规划  

---

## 6. 文档关系

| 文档 | 职责 |
|------|------|
| `README.md` | 产品说明、功能一览、使用与「当前开发目标」摘要 |
| `CONTRIBUTING.md` | 贡献流程、开发约定、执行基线细节 |
| **`ROADMAP.md`（本文）** | 跨版本里程碑与优先级路线图 |

历史「交接文档」中的会话流水、代理教训与本地绝对路径**不再维护**；工程约定已收敛到第四节与 CONTRIBUTING。
